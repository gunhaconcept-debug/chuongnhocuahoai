name: Sync all MonkeyD stories

on:
  workflow_dispatch:
  schedule:
    - cron: "15 20 * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: sync-monkeyd-team
  cancel-in-progress: false

jobs:
  sync:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install Playwright + Sharp
        run: |
          npm install playwright@1.55.0 sharp@0.34.3 --no-save
          npx playwright install --with-deps chromium

      - name: Sync MonkeyD team exactly
        run: node scripts/sync-team.mjs

      - name: Commit synced data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data truyen assets/covers
          git diff --cached --quiet || git commit -m "Sync MonkeyD team layout, rankings and audio links"
          git push

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: "."

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4